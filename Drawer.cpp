
#include <stdlib.h>
#include <iostream>
#include <stdio.h>
#include <conio.h> 
#include "Drawer.h"
#include "Bar.h"
#include "Ball.h"
//#include<windows.h>

/*

void Drawer::clear_screen() {
	system("cls");

}

void Drawer::print_on_screen(Bar& bar) {
	int x = static_cast<int>(bar.get_position().x);
	int y = static_cast<int>(bar.get_position().y);
	
	//COORD cursor_position = {x, y};
	SetCursorPos(x, y);
	//SetConsoleCursorPosition(m_hConsole, cursor_position);
	for (int i = 0; i < bar.) {
		std::cout << bar.get_symbol_type();

	}
	

}


void Drawer::print_on_screen(Ball& ball) {
	int x = static_cast<int>(ball.get_position().x);
	int y = static_cast<int>(ball.get_position().y);

	//COORD cursor_position = { x, y };
	SetCursorPos(x, y);
	//SetConsoleCursorPosition(m_hConsole, cursor_position);
	std::cout << ball.get_symbol_type();

}

void Drawer::print_on_screen(Wall& wall) {
	int x = static_cast<int>(wall.get_position().x);
	int y = static_cast<int>(wall.get_position().y);

	//COORD cursor_position = { x, y };
	SetCursorPos(x, y);
	//SetConsoleCursorPosition(m_hConsole, cursor_position);
	std::cout << wall.get_symbol_type();

}


*/